---
title: "Organize Data Notebook"
---

```{r}
#| label: setup
#| include: false
library(here)
source(here("utils","check_packages.R"))
source(here("utils","functions.R"))
```

```{r}
# Load the raw dataset
wdi_data <- read_csv(here("data","data_raw","wdi_data_original.csv"))

# Display the first few rows
head(wdi_data)

# Display a summary of the data
summary(wdi_data)

```
# Raw data to ---------> analytical data
# check yourself. before wreck yourself

# visualaize 
# Tidy data
# Key dvs- # narrow Down to One dependent variable
# Key IVS
#few important Covariables
# 

# data wrangling

#missing values
# check the missing values slides to impute process

# merging data

# reshaping data (not in may case)



```{r}
cleaned_dataset <- wdi_data

save(cleaned_dataset, file = here("data", "data_constructed", "analytical_data.RData"))

```

